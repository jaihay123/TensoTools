For this project to successfully run.

-The project needs to be opened in Python 3.8 or above.
-The correct libraries need to be installed.

To install the libraries, the requirements.txt file included can be run which automates the process.

Simply enter the command into the pip console in the directory of this project: pip install -r requirements.txt

This should allow you to run the program from the main.py script.